---
spring:
  config:
    activate:
      on-profile: local
jwt:
  screat-key: ${JWT_KEY}
  access-expiration-time: ${ACCESS_EXPIRATION_TIME}
  refresh-expiration-time: ${REFRESH_EXPIRATION_TIME}


---
spring:
  config:
    activate:
      on-profile: dev

jwt:
  screat-key: ${JWT_KEY}
  access-expiration-time: ${ACCESS_EXPIRATION_TIME}
  refresh-expiration-time: ${REFRESH_EXPIRATION_TIME}
