spring:
  profiles:
    group:
      local: common, domain, infra, security
      dev: common, domain, infra, security

server:
  shutdown: graceful

---
spring:
  config:
    activate:
      on-profile: local

---
spring:
  config:
    activate:
      on-profile: dev
